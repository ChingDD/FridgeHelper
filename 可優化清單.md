# 可優化清單

## SyncCoordinator.applyChanges — 每筆重新 fetch 的效能優化

### 現況
`applyChanges` 為了避免跨 `await` 沿用過時的本地快照（stale snapshot），改成每筆 record 處理前都重新呼叫 `localRepository.fetch()`。

- 優點：正確性提升，能看到同一次同步中剛新增/刪除的最新狀態，避免重複新增或漏刪。
- 缺點：從「1 次 fetch」變成「每筆一次 fetch」，N 筆變動就有 N 次查詢。

以目前家用冰箱的資料量（變動筆數少）來說，用效能換正確性是合理取捨。

### 未來優化方向
若之後資料量變大，可改用 `timeStamp → Item` 的字典（Dictionary）快取：

1. 迴圈開始前 fetch 一次，建立 `[String: Item]` 字典（key 為 `timeStamp`）。
2. 迴圈內以字典查找取代 `existingItems.first(where:)`，查找從 O(n) 降為 O(1)。
3. 在 `add` / `update` / `delete` 後同步維護字典（新增時寫入、刪除時移除）。

如此可兼顧正確性與「只查一次」的效能。

## Item.storeCondition legacy 對照在自訂儲存位置下會失準

### 現況
`Item` 在 `init` 與更新時，會用 `Item.legacyIndex(for:)` 把 `storeLocation` 反推成 legacy 的 `storeCondition`（0＝冷凍、1＝冷藏、2＝室溫）。

該對照只認得「冷凍」「室溫」兩個字串，其餘一律落到 `1`（冷藏）。使用者若把儲存位置改成「蔬果室」「冷凍櫃」之類的自訂名稱，`storeCondition` 就會全部變成冷藏。

`storeCondition` 目前只保留給舊版 CloudKit 相容，新邏輯一律讀 `storeLocation`，所以尚未造成可見問題。

### 為何值得記下來
階段 2A 讓儲存位置清單改為逐冰箱自訂後，使用者更容易建立非預設名稱的位置，踩到的機率會提高。

### 未來處理方向
確認舊版相容期結束後，直接移除 `storeCondition` 與 `legacyIndex(for:)`，不再維護這組對照。
